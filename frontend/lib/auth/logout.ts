export async function logoutUser(): Promise<void> {
  /*
  const res = await fetch("https://api.shareway.app/api/v1/auth/logout", {
    method: "POST",
    credentials: "include",
  });

  if (!res.ok) {
    throw new Error("Logout failed");
  }
  */

  console.log("Mock logoutUser() called – później podłączymy API.");
}
